% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/getBM.R
\name{getBM}
\alias{getBM}
\title{Retrieve gene/transcript annotations from Ensembl}
\usage{
getBM(attributes, filters = "", values = "", ..., species = NULL)
}
\arguments{
\item{attributes}{Attributes you want to retrieve.  A possible list of
attributes can be retrieved using the function \code{\link[biomaRt:listAttributes]{listAttributes()}}.}

\item{filters}{Filters (one or more) that should be used in the query.  A
possible list of filters can be retrieved using the function \code{\link[biomaRt:listFilters]{listFilters()}}.}

\item{values}{Values of the filter, e.g. vector of affy IDs.  If multiple
filters are specified then the argument should be a list of vectors of which
the position of each vector corresponds to the position of the filters in
the filters argument.}

\item{...}{Ignored. Used to catch no longer necessary parameters such as
\code{mart}, \code{checkFilters}, \code{verbose}, \code{uniqueRows}, \code{bmHeader}, \code{quote} and
\code{useCache} from \pkg{biomaRt} functions.}

\item{species}{Ensembl name (e.g. \code{"mouse"} or \code{"mus_musculus"}) of the
species to look Ensembl IDs for if, e.g., \code{external_gene_name} is provided
in \code{filters}. In \pkg{biomaRt}, this was inferred from the \code{mart}
argument, but since this argument is no longer used, the species must be
provided explicitly.}
}
\value{
A data frame containing the requested gene or transcript
annotations, with one column for each requested attribute.
The number of rows does not necessarily match the number of
requested identifiers, as some attributes can have multiple values
for a single identifier (e.g. a gene with multiple transcripts).
}
\description{
Retrieve gene/transcript annotations from Ensembl
}
\details{
Only a subset of the attributes and filters supported by the \code{biomaRt}
package are currently implemented, as this data has to be retrieved
through the Ensembl REST \code{lookup/id} endpoint rather than a generic
BioMart query engine.

Supported filters (only one can be used at a time): \code{ensembl_gene_id},
\code{ensembl_transcript_id}, \code{external_gene_name}.

Supported attributes: \code{ensembl_gene_id}, \code{ensembl_transcript_id},
\code{ensembl_peptide_id}, \code{external_gene_name}, \code{description},
\code{chromosome_name}, \code{start_position}, \code{end_position}, \code{strand},
\code{gene_biotype}, \code{transcript_biotype}, \code{version}.
}
\examples{
attribs <- c("ensembl_gene_id", "external_gene_name", "chromosome_name")
getBM(
  attributes = attribs,
  filters = "ensembl_gene_id",
  values = c("ENSG00000157764", "ENSG00000004939")
)

# It is also possible to a gene symbol, but a species must be specified, as
# gene symbols are not unique across species.
getBM(
  attributes = attribs,
  filters = "external_gene_name",
  values = c("APOE", "MAPT"),
  species = "human"
)
}
